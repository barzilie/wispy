import RecommendationsPanel from '../RecommendationsPanel/RecommendationsPanel';

/** Agentic AI panel (investigation + attack suggestions). */
const AgenticPanel = (props) => <RecommendationsPanel {...props} />;

export default AgenticPanel;
